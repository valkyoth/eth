[release]
version = "0.52.5"
policy = "independent"

[crates."eth-valkyoth-codec"]
previous_version = "0.21.0"
version = "0.21.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.5."

[crates."eth-valkyoth-primitives"]
previous_version = "0.11.4"
version = "0.11.4"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.5."

[crates."eth-valkyoth-hash"]
previous_version = "0.11.4"
version = "0.11.4"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.5."

[crates."eth-valkyoth-protocol"]
previous_version = "0.26.1"
version = "0.26.1"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.5."

[crates."eth-valkyoth-verify"]
previous_version = "0.25.0"
version = "0.26.0"
change = "code"
publish = true
reason = "Adds canonical account decoding, verified account capabilities, account-bound storage verification, and canonical absence and zero semantics."

[crates."eth-valkyoth-derive"]
previous_version = "0.17.5"
version = "0.17.5"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.5."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.7.7"
version = "0.7.7"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.5."

[crates."eth-valkyoth-evm-core"]
previous_version = "0.26.1"
version = "0.26.1"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.5."

[crates."eth-valkyoth-evm"]
previous_version = "0.10.2"
version = "0.10.2"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.5."

[crates."eth-valkyoth-rpc"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.5."

[crates."eth-valkyoth-signer"]
previous_version = "0.7.5"
version = "0.7.5"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.5."

[crates."eth-valkyoth-reth"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.5."

[crates."eth-valkyoth-testkit"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.5."

[crates.eth]
previous_version = "0.52.4"
version = "0.52.5"
change = "code"
publish = true
reason = "Exposes complete bounded account-to-storage proof composition with canonical account and zero-value semantics."
