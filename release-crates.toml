[release]
version = "0.37.3"
policy = "independent"

[crates."eth-valkyoth-codec"]
previous_version = "0.19.0"
version = "0.19.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.37.3."

[crates."eth-valkyoth-primitives"]
previous_version = "0.11.2"
version = "0.11.2"
change = "unchanged"
publish = false
reason = "No package changes for v0.37.3."

[crates."eth-valkyoth-hash"]
previous_version = "0.11.2"
version = "0.11.2"
change = "unchanged"
publish = false
reason = "No package changes for v0.37.3."

[crates."eth-valkyoth-protocol"]
previous_version = "0.25.2"
version = "0.25.2"
change = "unchanged"
publish = false
reason = "No package changes for v0.37.3."

[crates."eth-valkyoth-verify"]
previous_version = "0.20.2"
version = "0.21.0"
change = "code"
publish = true
reason = "Adds the recoverable secp256k1 backend boundary, optional k256 adapter feature, and backend-aware signature validation APIs."

[crates."eth-valkyoth-derive"]
previous_version = "0.17.2"
version = "0.17.2"
change = "unchanged"
publish = false
reason = "No package changes for v0.37.3."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.7.4"
version = "0.7.4"
change = "unchanged"
publish = false
reason = "No package changes for v0.37.3."

[crates."eth-valkyoth-evm"]
previous_version = "0.8.0"
version = "0.8.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.37.3."

[crates."eth-valkyoth-rpc"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.37.3."

[crates."eth-valkyoth-signer"]
previous_version = "0.7.3"
version = "0.7.3"
change = "unchanged"
publish = false
reason = "No package changes for v0.37.3."

[crates."eth-valkyoth-reth"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.37.3."

[crates."eth-valkyoth-testkit"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.37.3."

[crates.eth]
previous_version = "0.37.2"
version = "0.37.3"
change = "code"
publish = true
reason = "Exposes the explicit secp256k1-k256 facade feature and publishes v0.37.3 signature backend boundary documentation."
