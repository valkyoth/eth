[release]
version = "0.25.0"
policy = "independent"

[crates."eth-valkyoth-codec"]
previous_version = "0.16.0"
version = "0.17.0"
change = "code"
publish = true
reason = "Adds public RlpEncode and RlpDecode traits plus derive error support."

[crates."eth-valkyoth-primitives"]
previous_version = "0.10.1"
version = "0.11.0"
change = "code"
publish = true
reason = "Implements the public RLP traits for primitive domain types."

[crates."eth-valkyoth-hash"]
previous_version = "0.10.0"
version = "0.10.1"
change = "dependency"
publish = true
reason = "Depends on eth-valkyoth-primitives 0.11.0."

[crates."eth-valkyoth-protocol"]
previous_version = "0.22.0"
version = "0.22.1"
change = "dependency"
publish = true
reason = "Depends on eth-valkyoth-codec 0.17.0 and eth-valkyoth-primitives 0.11.0."

[crates."eth-valkyoth-verify"]
previous_version = "0.14.1"
version = "0.14.2"
change = "dependency"
publish = true
reason = "Depends on the v0.25.0-aligned codec, primitive, hash, and protocol crates."

[crates."eth-valkyoth-derive"]
previous_version = "0.16.1"
version = "0.17.0"
change = "code"
publish = true
reason = "Publishes reviewed RlpEncode and RlpDecode derive macros with trybuild diagnostics."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.7.1"
version = "0.7.2"
change = "dependency"
publish = true
reason = "Depends on eth-valkyoth-derive 0.17.0 for the optional derive feature."

[crates."eth-valkyoth-evm"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.25.0."

[crates."eth-valkyoth-rpc"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.25.0."

[crates."eth-valkyoth-signer"]
previous_version = "0.7.0"
version = "0.7.1"
change = "dependency"
publish = true
reason = "Depends on eth-valkyoth-primitives 0.11.0."

[crates."eth-valkyoth-reth"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.25.0."

[crates."eth-valkyoth-testkit"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.25.0."

[crates.eth]
previous_version = "0.24.2"
version = "0.25.0"
change = "code"
publish = true
reason = "Re-exports the public RLP trait surface and updates packaged documentation."
