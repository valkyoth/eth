[release]
version = "0.13.0"
policy = "independent"

[crates."eth-valkyoth-codec"]
previous_version = "0.10.0"
version = "0.10.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.13.0."

[crates."eth-valkyoth-primitives"]
previous_version = "0.10.0"
version = "0.10.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.13.0."

[crates."eth-valkyoth-hash"]
previous_version = "0.10.0"
version = "0.10.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.13.0."

[crates."eth-valkyoth-protocol"]
previous_version = "0.12.0"
version = "0.13.0"
change = "code"
publish = true
reason = "Adds the v0.13.0 unvalidated EIP-2930 access-list transaction decoder."

[crates."eth-valkyoth-verify"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.13.0."

[crates."eth-valkyoth-derive"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.13.0."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.13.0."

[crates."eth-valkyoth-evm"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.13.0."

[crates."eth-valkyoth-rpc"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.13.0."

[crates."eth-valkyoth-signer"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.13.0."

[crates."eth-valkyoth-reth"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.13.0."

[crates."eth-valkyoth-testkit"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.13.0."

[crates.eth]
previous_version = "0.12.0"
version = "0.13.0"
change = "code"
publish = true
reason = "Updates facade documentation, error re-exports, and dependency ranges for the v0.13.0 access-list transaction decoder."
