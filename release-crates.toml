[release]
version = "0.5.0"
policy = "independent"

[crates."eth-valkyoth-codec"]
previous_version = "0.4.0"
version = "0.5.0"
change = "code"
publish = true
reason = "Adds mandatory proof-node and cumulative item budgets plus checked length and offset helpers."

[crates."eth-valkyoth-primitives"]
previous_version = "0.4.0"
version = "0.4.0"
change = "unchanged"
publish = false
reason = "Primitive domains remain unchanged for the v0.5.0 decode-budget milestone."

[crates."eth-valkyoth-protocol"]
previous_version = "0.4.0"
version = "0.4.0"
change = "unchanged"
publish = false
reason = "Protocol state remains unchanged for the v0.5.0 decode-budget milestone."

[crates."eth-valkyoth-verify"]
previous_version = "0.4.0"
version = "0.4.0"
change = "unchanged"
publish = false
reason = "Verification boundaries remain unchanged for the v0.5.0 decode-budget milestone."

[crates."eth-valkyoth-derive"]
previous_version = "0.4.0"
version = "0.4.0"
change = "unchanged"
publish = false
reason = "Derive macros remain unchanged for the v0.5.0 decode-budget milestone."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.4.0"
version = "0.4.1"
change = "dependency"
publish = true
reason = "Updates the optional sanitization dependency to the current 1.1.1 release."

[crates."eth-valkyoth-evm"]
previous_version = "0.3.0"
version = "0.3.0"
change = "unchanged"
publish = false
reason = "EVM boundary remains unchanged for the v0.5.0 decode-budget milestone."

[crates."eth-valkyoth-rpc"]
previous_version = "0.3.0"
version = "0.3.0"
change = "unchanged"
publish = false
reason = "RPC boundary remains unchanged for the v0.5.0 decode-budget milestone."

[crates."eth-valkyoth-signer"]
previous_version = "0.3.1"
version = "0.3.1"
change = "unchanged"
publish = false
reason = "Signer boundary remains unchanged for the v0.5.0 decode-budget milestone."

[crates."eth-valkyoth-reth"]
previous_version = "0.3.0"
version = "0.3.0"
change = "unchanged"
publish = false
reason = "Reth boundary remains unchanged for the v0.5.0 decode-budget milestone."

[crates."eth-valkyoth-testkit"]
previous_version = "0.3.0"
version = "0.3.0"
change = "unchanged"
publish = false
reason = "Testkit boundary remains unchanged for the v0.5.0 decode-budget milestone."

[crates.eth]
previous_version = "0.4.0"
version = "0.5.0"
change = "code"
publish = true
reason = "Re-exports the expanded decode-budget API through the facade crate."
