[release]
version = "0.52.2"
policy = "independent"

[crates."eth-valkyoth-codec"]
previous_version = "0.19.0"
version = "0.19.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.2."

[crates."eth-valkyoth-primitives"]
previous_version = "0.11.2"
version = "0.11.2"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.2."

[crates."eth-valkyoth-hash"]
previous_version = "0.11.2"
version = "0.11.2"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.2."

[crates."eth-valkyoth-protocol"]
previous_version = "0.25.2"
version = "0.25.2"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.2."

[crates."eth-valkyoth-verify"]
previous_version = "0.23.0"
version = "0.23.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.2."

[crates."eth-valkyoth-derive"]
previous_version = "0.17.3"
version = "0.17.3"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.2."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.7.5"
version = "0.7.5"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.2."

[crates."eth-valkyoth-evm-core"]
previous_version = "0.26.0"
version = "0.27.0"
change = "code"
publish = true
reason = "Corrects consensus handling of truncated PUSH immediates, shares instruction advancement with jump-destination analysis, and retains the legacy error variant for facade compatibility."

[crates."eth-valkyoth-evm"]
previous_version = "0.10.0"
version = "0.10.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.2."

[crates."eth-valkyoth-rpc"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.2."

[crates."eth-valkyoth-signer"]
previous_version = "0.7.3"
version = "0.7.3"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.2."

[crates."eth-valkyoth-reth"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.2."

[crates."eth-valkyoth-testkit"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.52.2."

[crates.eth]
previous_version = "0.52.1"
version = "0.52.2"
change = "code"
publish = true
reason = "Updates evm-core to 0.27.0, exposes consensus-correct truncated PUSH execution, and preserves the legacy public error variant for patch compatibility."
