[release]
version = "0.9.1"
policy = "independent"

[crates."eth-valkyoth-codec"]
previous_version = "0.9.0"
version = "0.9.1"
change = "code"
publish = true
reason = "Adds public canonical RLP integer payload helpers used by primitive domains."

[crates."eth-valkyoth-primitives"]
previous_version = "0.8.0"
version = "0.9.1"
change = "code"
publish = true
reason = "Delegates canonical RLP integer payload parsing to eth-valkyoth-codec."

[crates."eth-valkyoth-protocol"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.1."

[crates."eth-valkyoth-verify"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.1."

[crates."eth-valkyoth-derive"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.1."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.1."

[crates."eth-valkyoth-evm"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.1."

[crates."eth-valkyoth-rpc"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.1."

[crates."eth-valkyoth-signer"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.1."

[crates."eth-valkyoth-reth"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.1."

[crates."eth-valkyoth-testkit"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.1."

[crates.eth]
previous_version = "0.9.0"
version = "0.9.1"
change = "dependency"
publish = true
reason = "Bumps facade dependencies to the v0.9.1 codec and primitive packages."
