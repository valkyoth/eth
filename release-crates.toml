[release]
version = "0.50.2"
policy = "independent"

[crates."eth-valkyoth-codec"]
previous_version = "0.19.0"
version = "0.19.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.50.2."

[crates."eth-valkyoth-primitives"]
previous_version = "0.11.2"
version = "0.11.2"
change = "unchanged"
publish = false
reason = "No package changes for v0.50.2."

[crates."eth-valkyoth-hash"]
previous_version = "0.11.2"
version = "0.11.2"
change = "unchanged"
publish = false
reason = "No package changes for v0.50.2."

[crates."eth-valkyoth-protocol"]
previous_version = "0.25.2"
version = "0.25.2"
change = "unchanged"
publish = false
reason = "No package changes for v0.50.2."

[crates."eth-valkyoth-verify"]
previous_version = "0.21.0"
version = "0.21.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.50.2."

[crates."eth-valkyoth-derive"]
previous_version = "0.17.2"
version = "0.17.2"
change = "unchanged"
publish = false
reason = "No package changes for v0.50.2."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.7.4"
version = "0.7.4"
change = "unchanged"
publish = false
reason = "No package changes for v0.50.2."

[crates."eth-valkyoth-evm-core"]
previous_version = "0.14.0"
version = "0.15.0"
change = "code"
publish = true
reason = "Adds the dependency-free BN254 Fp6/Fp12 tower foundation and bounded internal tower accumulation shape while non-empty pairing algebra remains fail-closed."

[crates."eth-valkyoth-evm"]
previous_version = "0.10.0"
version = "0.10.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.50.2."

[crates."eth-valkyoth-rpc"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.50.2."

[crates."eth-valkyoth-signer"]
previous_version = "0.7.3"
version = "0.7.3"
change = "unchanged"
publish = false
reason = "No package changes for v0.50.2."

[crates."eth-valkyoth-reth"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.50.2."

[crates."eth-valkyoth-testkit"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.50.2."

[crates.eth]
previous_version = "0.50.1"
version = "0.50.2"
change = "code"
publish = true
reason = "Updates the optional evm-core dependency to eth-valkyoth-evm-core 0.15.0 and documents the BN254 Fp6/Fp12 tower foundation."
