[release]
version = "0.26.0"
policy = "independent"

[crates."eth-valkyoth-codec"]
previous_version = "0.17.0"
version = "0.17.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.26.0."

[crates."eth-valkyoth-primitives"]
previous_version = "0.11.0"
version = "0.11.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.26.0."

[crates."eth-valkyoth-hash"]
previous_version = "0.10.1"
version = "0.10.1"
change = "unchanged"
publish = false
reason = "No package changes for v0.26.0."

[crates."eth-valkyoth-protocol"]
previous_version = "0.22.1"
version = "0.22.1"
change = "unchanged"
publish = false
reason = "No package changes for v0.26.0."

[crates."eth-valkyoth-verify"]
previous_version = "0.14.2"
version = "0.15.0"
change = "code"
publish = true
reason = "Adds the no-alloc EIP-712 typed-data encoder and official Ether Mail KAT."

[crates."eth-valkyoth-derive"]
previous_version = "0.17.0"
version = "0.17.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.26.0."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.7.2"
version = "0.7.2"
change = "unchanged"
publish = false
reason = "No package changes for v0.26.0."

[crates."eth-valkyoth-evm"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.26.0."

[crates."eth-valkyoth-rpc"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.26.0."

[crates."eth-valkyoth-signer"]
previous_version = "0.7.1"
version = "0.7.1"
change = "unchanged"
publish = false
reason = "No package changes for v0.26.0."

[crates."eth-valkyoth-reth"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.26.0."

[crates."eth-valkyoth-testkit"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.26.0."

[crates.eth]
previous_version = "0.25.0"
version = "0.26.0"
change = "code"
publish = true
reason = "Re-exports the EIP-712 typed-data encoder and updates packaged documentation."
