[release]
version = "0.6.0"
policy = "independent"

[crates."eth-valkyoth-codec"]
previous_version = "0.5.0"
version = "0.6.0"
change = "code"
publish = true
reason = "Adds the canonical RLP scalar decoder with exact-consumption and malformed length checks."

[crates."eth-valkyoth-primitives"]
previous_version = "0.5.0"
version = "0.5.0"
change = "unchanged"
publish = false
reason = "Primitive domains remain unchanged for the initial v0.6.0 dependency refresh."

[crates."eth-valkyoth-protocol"]
previous_version = "0.5.0"
version = "0.5.0"
change = "unchanged"
publish = false
reason = "Protocol state remains unchanged for the initial v0.6.0 dependency refresh."

[crates."eth-valkyoth-verify"]
previous_version = "0.5.0"
version = "0.5.0"
change = "unchanged"
publish = false
reason = "Verification boundaries remain unchanged for the initial v0.6.0 dependency refresh."

[crates."eth-valkyoth-derive"]
previous_version = "0.5.0"
version = "0.6.0"
change = "code"
publish = true
reason = "Starts v0.6.0 by refreshing derive macro dependencies, including quote 1.0.46."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.5.0"
version = "0.6.0"
change = "code"
publish = true
reason = "Starts v0.6.0 by refreshing optional sanitization support to sanitization 1.2.2."

[crates."eth-valkyoth-evm"]
previous_version = "0.3.0"
version = "0.3.0"
change = "unchanged"
publish = false
reason = "EVM boundary remains unchanged for the v0.5.0 decode-budget milestone."

[crates."eth-valkyoth-rpc"]
previous_version = "0.3.0"
version = "0.3.0"
change = "unchanged"
publish = false
reason = "RPC boundary remains unchanged for the v0.5.0 decode-budget milestone."

[crates."eth-valkyoth-signer"]
previous_version = "0.3.2"
version = "0.3.2"
change = "unchanged"
publish = false
reason = "Signer remains on the published v0.3.2 dependency range."

[crates."eth-valkyoth-reth"]
previous_version = "0.3.0"
version = "0.3.0"
change = "unchanged"
publish = false
reason = "Reth boundary remains unchanged for the v0.5.0 decode-budget milestone."

[crates."eth-valkyoth-testkit"]
previous_version = "0.3.0"
version = "0.3.0"
change = "unchanged"
publish = false
reason = "Testkit boundary remains unchanged for the v0.5.0 decode-budget milestone."

[crates.eth]
previous_version = "0.5.0"
version = "0.6.0"
change = "code"
publish = true
reason = "Starts v0.6.0 by exposing the refreshed optional sanitization dependency range."
