[release]
version = "0.18.0"
policy = "independent"

[crates."eth-valkyoth-codec"]
previous_version = "0.16.0"
version = "0.16.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.18.0."

[crates."eth-valkyoth-primitives"]
previous_version = "0.10.1"
version = "0.10.1"
change = "unchanged"
publish = false
reason = "No package changes for v0.18.0."

[crates."eth-valkyoth-hash"]
previous_version = "0.10.0"
version = "0.10.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.18.0."

[crates."eth-valkyoth-protocol"]
previous_version = "0.17.0"
version = "0.18.0"
change = "code"
publish = true
reason = "Adds proof-gated transaction validation typestate transitions."

[crates."eth-valkyoth-verify"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.18.0."

[crates."eth-valkyoth-derive"]
previous_version = "0.16.1"
version = "0.16.1"
change = "unchanged"
publish = false
reason = "No package changes for v0.18.0."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.7.1"
version = "0.7.1"
change = "unchanged"
publish = false
reason = "No package changes for v0.18.0."

[crates."eth-valkyoth-evm"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.18.0."

[crates."eth-valkyoth-rpc"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.18.0."

[crates."eth-valkyoth-signer"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.18.0."

[crates."eth-valkyoth-reth"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.18.0."

[crates."eth-valkyoth-testkit"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.18.0."

[crates.eth]
previous_version = "0.17.0"
version = "0.18.0"
change = "code"
publish = true
reason = "Re-exports the v0.18.0 proof-gated transaction typestate APIs and updates packaged documentation."
