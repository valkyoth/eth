[release]
version = "0.10.0"
policy = "independent"

[crates."eth-valkyoth-codec"]
previous_version = "0.9.1"
version = "0.10.0"
change = "code"
publish = true
reason = "Adds v0.10.0 package documentation for the RLP fuzz harness baseline and parser seed-corpus process."

[crates."eth-valkyoth-primitives"]
previous_version = "0.9.2"
version = "0.10.0"
change = "code"
publish = true
reason = "Adds signed ChainId decode helpers, release-visible unexpected codec errors, unambiguous transaction type constructors, and the codec 0.10.0 dependency range."

[crates."eth-valkyoth-hash"]
previous_version = "0.9.3"
version = "0.10.0"
change = "code"
publish = true
reason = "Adds Keccak-256 empty-input KAT constants and backend conformance helper API."

[crates."eth-valkyoth-protocol"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.10.0."

[crates."eth-valkyoth-verify"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.10.0."

[crates."eth-valkyoth-derive"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.10.0."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.10.0."

[crates."eth-valkyoth-evm"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.10.0."

[crates."eth-valkyoth-rpc"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.10.0."

[crates."eth-valkyoth-signer"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.10.0."

[crates."eth-valkyoth-reth"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.10.0."

[crates."eth-valkyoth-testkit"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.10.0."

[crates.eth]
previous_version = "0.9.3"
version = "0.10.0"
change = "code"
publish = true
reason = "Updates facade documentation and dependency ranges for the v0.10.0 RLP fuzz harness baseline."
