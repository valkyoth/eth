[release]
version = "0.7.0"
policy = "independent"

[crates."eth-valkyoth-codec"]
previous_version = "0.6.0"
version = "0.7.0"
change = "code"
publish = true
reason = "Adds bounded canonical RLP list decoding with nesting, item-count enforcement, and immediate child iteration."

[crates."eth-valkyoth-primitives"]
previous_version = "0.5.0"
version = "0.7.0"
change = "metadata"
publish = true
reason = "Republishes corrected MIT OR Apache-2.0 license metadata for crates.io."

[crates."eth-valkyoth-protocol"]
previous_version = "0.5.0"
version = "0.7.0"
change = "metadata"
publish = true
reason = "Republishes corrected MIT OR Apache-2.0 license metadata for crates.io."

[crates."eth-valkyoth-verify"]
previous_version = "0.5.0"
version = "0.7.0"
change = "metadata"
publish = true
reason = "Republishes corrected MIT OR Apache-2.0 license metadata for crates.io."

[crates."eth-valkyoth-derive"]
previous_version = "0.6.0"
version = "0.7.0"
change = "metadata"
publish = true
reason = "Republishes corrected MIT OR Apache-2.0 license metadata for crates.io."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.6.0"
version = "0.7.0"
change = "metadata"
publish = true
reason = "Republishes corrected MIT OR Apache-2.0 license metadata for crates.io."

[crates."eth-valkyoth-evm"]
previous_version = "0.3.0"
version = "0.7.0"
change = "metadata"
publish = true
reason = "Republishes corrected MIT OR Apache-2.0 license metadata for crates.io."

[crates."eth-valkyoth-rpc"]
previous_version = "0.6.0"
version = "0.7.0"
change = "metadata"
publish = true
reason = "Republishes corrected MIT OR Apache-2.0 license metadata for crates.io."

[crates."eth-valkyoth-signer"]
previous_version = "0.3.2"
version = "0.7.0"
change = "metadata"
publish = true
reason = "Republishes corrected MIT OR Apache-2.0 license metadata for crates.io."

[crates."eth-valkyoth-reth"]
previous_version = "0.3.0"
version = "0.7.0"
change = "metadata"
publish = true
reason = "Republishes corrected MIT OR Apache-2.0 license metadata for crates.io."

[crates."eth-valkyoth-testkit"]
previous_version = "0.3.0"
version = "0.7.0"
change = "metadata"
publish = true
reason = "Republishes corrected MIT OR Apache-2.0 license metadata for crates.io."

[crates.eth]
previous_version = "0.6.0"
version = "0.7.0"
change = "code"
publish = true
reason = "Exposes bounded canonical RLP list decoding and item iteration through the facade crate."
