[release]
version = "0.57.0"
milestone = "0.57.0"
baseline = "0.55.0"
review_baseline = "0.56.0"
cumulative_milestones = ["0.56.0", "0.57.0"]
policy = "independent"
stage = "internal"

[crates."eth-valkyoth-codec"]
previous_version = "0.21.0"
version = "0.21.0"
change = "unchanged"
publish = false
reason = "Retains published version during the internal release train."

[crates."eth-valkyoth-primitives"]
previous_version = "0.11.4"
version = "0.11.4"
change = "unchanged"
publish = false
reason = "Retains published version during the internal release train."

[crates."eth-valkyoth-hash"]
previous_version = "0.11.4"
version = "0.11.4"
change = "unchanged"
publish = false
reason = "Retains published version during the internal release train."

[crates."eth-valkyoth-protocol"]
previous_version = "0.26.1"
version = "0.26.1"
change = "unchanged"
publish = false
reason = "Retains published version during the internal release train."

[crates."eth-valkyoth-verify"]
previous_version = "0.27.0"
version = "0.27.0"
change = "unchanged"
publish = false
reason = "Retains published version during the internal release train."

[crates."eth-valkyoth-derive"]
previous_version = "0.18.0"
version = "0.18.0"
change = "dependency"
publish = false
reason = "Workspace dependency maintenance is accumulated for v0.60.0."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.8.0"
version = "0.8.0"
change = "dependency"
publish = false
reason = "Workspace dependency maintenance is accumulated for v0.60.0."

[crates."eth-valkyoth-evm-core"]
previous_version = "0.29.0"
version = "0.29.0"
change = "code"
publish = false
reason = "Adds public-input BLS12-381 Fp and on-curve G1 arithmetic; cumulative bump at v0.60.0."

[crates."eth-valkyoth-evm"]
previous_version = "0.12.2"
version = "0.12.2"
change = "unchanged"
publish = false
reason = "Retains published version during the internal release train."

[crates."eth-valkyoth-rpc"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "Retains published version during the internal release train."

[crates."eth-valkyoth-signer"]
previous_version = "0.7.5"
version = "0.7.5"
change = "unchanged"
publish = false
reason = "Retains published version during the internal release train."

[crates."eth-valkyoth-reth"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "Retains published version during the internal release train."

[crates."eth-valkyoth-testkit"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "Retains published version during the internal release train."

[crates."eth"]
previous_version = "0.56.0"
version = "0.57.0"
change = "code"
publish = false
reason = "Internal facade milestone for public-input BLS12-381 G1 arithmetic; no publication before v0.60.0."
