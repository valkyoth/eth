[release]
version = "0.24.1"
policy = "independent"

[crates."eth-valkyoth-codec"]
previous_version = "0.16.0"
version = "0.16.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.24.1."

[crates."eth-valkyoth-primitives"]
previous_version = "0.10.1"
version = "0.10.1"
change = "unchanged"
publish = false
reason = "No package changes for v0.24.1."

[crates."eth-valkyoth-hash"]
previous_version = "0.10.0"
version = "0.10.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.24.1."

[crates."eth-valkyoth-protocol"]
previous_version = "0.20.0"
version = "0.21.0"
change = "code"
publish = true
reason = "Adds EIP-7702 set-code transaction and authorization signing preimage helpers."

[crates."eth-valkyoth-verify"]
previous_version = "0.13.0"
version = "0.14.0"
change = "code"
publish = true
reason = "Adds EIP-7702 set-code transaction signature validation and authorization signature validation APIs."

[crates."eth-valkyoth-derive"]
previous_version = "0.16.1"
version = "0.16.1"
change = "unchanged"
publish = false
reason = "No package changes for v0.24.1."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.7.1"
version = "0.7.1"
change = "unchanged"
publish = false
reason = "No package changes for v0.24.1."

[crates."eth-valkyoth-evm"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.24.1."

[crates."eth-valkyoth-rpc"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.24.1."

[crates."eth-valkyoth-signer"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.24.1."

[crates."eth-valkyoth-reth"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.24.1."

[crates."eth-valkyoth-testkit"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.24.1."

[crates.eth]
previous_version = "0.24.0"
version = "0.24.1"
change = "code"
publish = true
reason = "Re-exports EIP-7702 set-code signing and authorization validation APIs and updates packaged documentation."
