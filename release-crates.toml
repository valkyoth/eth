[release]
version = "0.30.0"
policy = "independent"

[crates."eth-valkyoth-codec"]
previous_version = "0.17.0"
version = "0.17.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.30.0."

[crates."eth-valkyoth-primitives"]
previous_version = "0.11.0"
version = "0.11.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.30.0."

[crates."eth-valkyoth-hash"]
previous_version = "0.11.0"
version = "0.11.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.30.0."

[crates."eth-valkyoth-protocol"]
previous_version = "0.24.0"
version = "0.25.0"
change = "code"
publish = true
reason = "Adds syntactic EIP-4895 withdrawal-list decoding with borrowed entries and fuzz coverage."

[crates."eth-valkyoth-verify"]
previous_version = "0.17.2"
version = "0.17.3"
change = "dependency"
publish = true
reason = "Bumps the published protocol dependency range to eth-valkyoth-protocol 0.25.0."

[crates."eth-valkyoth-derive"]
previous_version = "0.17.0"
version = "0.17.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.30.0."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.7.2"
version = "0.7.2"
change = "unchanged"
publish = false
reason = "No package changes for v0.30.0."

[crates."eth-valkyoth-evm"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.30.0."

[crates."eth-valkyoth-rpc"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.30.0."

[crates."eth-valkyoth-signer"]
previous_version = "0.7.1"
version = "0.7.1"
change = "unchanged"
publish = false
reason = "No package changes for v0.30.0."

[crates."eth-valkyoth-reth"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.30.0."

[crates."eth-valkyoth-testkit"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.30.0."

[crates.eth]
previous_version = "0.29.0"
version = "0.30.0"
change = "code"
publish = true
reason = "Re-exports the new withdrawal decode APIs and updates packaged documentation."
