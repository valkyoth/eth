[release]
version = "0.9.2"
policy = "independent"

[crates."eth-valkyoth-codec"]
previous_version = "0.9.1"
version = "0.9.1"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.2."

[crates."eth-valkyoth-primitives"]
previous_version = "0.9.1"
version = "0.9.2"
change = "code"
publish = true
reason = "Adds no-alloc RLP encode and exact-decode helpers for primitive domains."

[crates."eth-valkyoth-protocol"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.2."

[crates."eth-valkyoth-verify"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.2."

[crates."eth-valkyoth-derive"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.2."

[crates."eth-valkyoth-sanitization"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.2."

[crates."eth-valkyoth-evm"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.2."

[crates."eth-valkyoth-rpc"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.2."

[crates."eth-valkyoth-signer"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.2."

[crates."eth-valkyoth-reth"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.2."

[crates."eth-valkyoth-testkit"]
previous_version = "0.7.0"
version = "0.7.0"
change = "unchanged"
publish = false
reason = "No package changes for v0.9.2."

[crates.eth]
previous_version = "0.9.1"
version = "0.9.2"
change = "dependency"
publish = true
reason = "Bumps facade dependency to the v0.9.2 primitive RLP bridge package."
