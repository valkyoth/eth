#![no_std]
#![forbid(unsafe_code)]
//! Future Reth integration boundary for `eth`.

/// Placeholder proving Reth integration stays outside the protocol core.
#[derive(Clone, Copy, Debug, Eq, PartialEq)]
pub struct RethAdapterBoundary;
