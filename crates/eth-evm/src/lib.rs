#![no_std]
#![forbid(unsafe_code)]
//! Future REVM adapter boundary for `eth`.

/// Placeholder proving the EVM adapter is explicit and feature-gated.
#[derive(Clone, Copy, Debug, Eq, PartialEq)]
pub struct EvmAdapterBoundary;
